//BE NAME DOOST
#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#define vertex pair<int,int>
#define X first
#define Y second
using namespace std;

int n,r;
pair<vertex ,vertex > v[5009];
int shib
int main()
{
	cin>>n;
	for(int i=0;i<n;i++)
		cin>>v[i].first.X>>v[i].first.Y>>v[i].second.X>>v[i].second.Y;
	for(int i=0;i<n-1;i++)
		for(int j=i+1;j<n;j++)
		{
			if(v[i].first==v[j].first)
			{

			}
		}
}
