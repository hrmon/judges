//BE NAME DOOST
#include <iostream>
#include <vector>
#include <set>
#include <algorithm>
#define X first.first
#define Y first.second
#define mp make_pair
#define ind second
using namespace std;
const int MAXN=50000+9;
vector<pair<pair<int,int>,int> > r,t;
int n,mf[MAXN],me[MAXN];

bool dfs(int node)
{

}

int main()
{
	cin>>n;
	for(int i=0;i<n;i++)
	{
		int a,b;
		cin>>a>>b;
		R.push_back(mp(mp(a,b),i+1));
	}
	sort(r.begin(),r.end());
	reverse(r.begin(),r.end());
//////########################///////
	t[0]
	for(int i=1;i<n;i++)

}

